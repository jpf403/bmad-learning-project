---
topic: Barbershop Appointment Scheduler — PRD
updated: 2026-07-21T00:00
---

- (event) PRD workflow started; python/uv unavailable in this environment, memlog maintained by hand in script-equivalent format
- (note) source input: brief-bmad-learning-project-2026-07-20 (brief.md, addendum.md, .memlog.md) — status final, full solution/scope/success-criteria/who-serves/deliverables already decided there
- (decision by user) stakes calibration: full rigor, treat as any normal project — do not scale down for hobby/demo status despite the brief's internal framing
- (decision by user) working mode: coaching path
- (decision by user) coaching entry point: journey-led (customer/barber/admin sessions first, FRs formalized off the back of those)
- (note) clarified for user: in this method's flow, PRD precedes UX (PRD -> UX -> Architecture -> Epics/Stories); no pivot needed
- (event) UJ-1 captured: customer signed-in booking (home -> sign in -> schedule appointment -> select barber -> calendar date pick -> time dropdown -> submit)
- (decision by user) guest booking path (introduced during UJ narration) stays OUT of PRD scope, consistent with brief's "any logged-in user" framing — flagged as a possible same-day scope addition, not committed yet
- (event) UJ-2 captured: barber dashboard (sign-in routes directly to own schedule, defaults to today, prev/next-day arrows, full 9:00-4:30 30-min slot list rendered with customer name next to booked slots, barber-own-appointments only, can also book via shared booking page)
- (event) UJ-3 captured: admin (same schedule view as barber + Select Barber dropdown to view any barber's day; separate Accounts tab -> search/select user -> edit permission level via dropdown (customer/barber/admin) -> Create Account button for direct barber/admin account creation)
- (event) UJ-4 captured: new customer registration (home page Create Account -> email, password, first name, last name -> submit creates account)
- (event) all 4 user journeys confirmed; moving to FR formalization off journeys + brief
- (decision by user) FR1-FR21 confirmed as drafted (Authentication & Accounts, Booking, Barber Dashboard, Admin Dashboard & Account Management, Shared Pages)
- (decision by user) SCOPE REVERSAL vs. finalized brief: mobile responsiveness is now IN scope (brief's Scope section explicitly listed it as out-of-scope) -- site must look clean on both mobile and desktop; specific concern is hover-dependent interactions (e.g. home page CTA hover) causing the known touch "double-tap to activate" bug -- must be fixed by removing hover-dependence and/or testing single-tap works on touch
- (note) this conflicts with brief.md Scope section as currently written; flag for input reconciliation at Finalize -- brief itself is not being edited in this run, PRD supersedes it on this point
- (decision by user) soft personal deadline moved from "this Friday" to next Friday (2026-07-31); realistic outer bound of ~1.5 workweeks from brief still stands as the hard ceiling -- reflect in Deliverables & Timeline section
- (decision by user) FR18 expanded: admin can edit ALL account fields for any user (not just permission level) -- email, first name, last name, password. Admin sets password directly (no reset-link/email mechanism, no forced-change-on-next-login flag). No audit trail required. This is now the de facto password-reset mechanism (self-service password reset remains explicitly out of scope per brief)
- (decision by user) Success Metrics table (7 metrics + counter-metrics) confirmed as drafted
- (note) user deferred ERD to the architecture phase (bmad-architecture / Winston) rather than this PRD run
- (event) explained hashing (password-at-rest storage) vs. tokens (session/auth-after-login) distinction to user
- (decision by user) session mechanism leaning: token-based (e.g. JWT) over server cookie sessions -- captured in addendum.md as a leaning, not locked, final call deferred to Architecture
- (decision by user) FR23 added: logout capability for all roles (customer, barber, admin) -- overlooked earlier as "too basic" but is in scope
- (event) created addendum.md for this PRD run with the auth session leaning
- (event) NFR1-NFR7 confirmed as drafted (security wording loosened to defer session mechanism to architecture)
- (event) moving to Out of Scope / Non-Goals section
- (event) Out of Scope / Non-Goals confirmed as drafted
- (event) full prd.md draft assembled and saved: Overview, Goals & Success Metrics, User Journeys (UJ-1-4), Functional Requirements (FR1-23), NFRs & Quality/DORA Practices, Out of Scope, Deliverables & Timeline, Open Items
- (decision by user) REFRAME: strip all "outside the story" / assignment-for-boss framing from the PRD -- PRD now reads purely as an in-universe barbershop product document (removed: two-frame Who This Serves, "vehicle not the point" framing, process-reflection deliverable and its success metric, "boss"/"walkthrough" language). This diverges intentionally from brief.md, which retains the dual frame -- brief.md is NOT being edited to match. The written process reflection remains a real deliverable outside this PRD's scope; it's just no longer narrated inside the PRD document itself.
- (decision by user) Success metric added: secure auth + sessions maintained correctly across page navigation, with counter-metric that a signed-out/back-button session can't reach protected pages
- (decision by user) FR24 added: Schedule Appointment page shows the signed-in user's own booked appointments as a list at the bottom
- (decision by user) FR25 added: customer can cancel their own appointment from that list, freeing the slot
- (decision by user) FR26 added: barber can cancel any appointment from their schedule view, freeing the slot
- (decision by user) FR27 added: admin can cancel appointments the same way a barber can (incl. via Select Barber dropdown)
- (note) no cancellation notifications sent -- covered by existing "no notifications" out-of-scope bullet; user acknowledged a real site would notify but this one won't
- (decision by user) FR1 and FR18 updated: password entry requires typing it twice to confirm (registration and admin-set-password both)
- (decision by user) FR28 added: new Account page where a signed-in user edits their own first/last name and password (confirm required) but NOT email
- (decision by user) FR29 clarified and confirmed (assumption tag removed): signed-out shows Login + Register buttons (no dropdown); signed-in shows a profile icon opening a dropdown with Account + Logout
- (decision by user) FR3 updated: Schedule Appointment nav tab is hidden entirely for signed-out users
- (decision by user) FR5 updated: signed-out user clicking a Schedule/booking CTA (e.g. home page) is redirected to Login page, not the booking page
- (decision by user) Deliverables & Timeline: removed "week and a half" outer-bound language, soft deadline of Friday 2026-07-31 is now the only timeline statement
- (event) memlog audit completed with user: all entries traced to prd.md/addendum.md; user opted to leave UJ-1-4 narratives as-is (not retrofitted with cancellation/account-page/nav additions) since FRs are the source of truth
- (decision by user) FR30 added: every cancellation (FR25, FR26, FR27) requires an explicit confirm step before it takes effect
- (event) input reconciliation subagent run against brief.md/addendum.md -> reconcile-brief.md; 1 real gap (testing-tooling leaning not carried to PRD addendum), 1 false alarm (timeline outer-bound removal, already an explicit logged user decision)
- (decision by user) testing-tooling leaning (xUnit+WebApplicationFactory, Vitest/Jest+RTL, optional Playwright) carried forward into this PRD's addendum.md, same as brief's addendum -- informational, not locked
- (event) Reviewer Gate run: rubric walker + adversarial-general + edge-case-hunter, all 3 in parallel -> review-rubric.md, review-adversarial-general.md, review-edge-case-hunter.md (47 raw findings, heavy overlap, synthesized into 8 high-tier + 12 medium/low tail for user)
- (decision by user) FR31 added: system seeds one admin account on first startup from server-side config if none exists -- addendum captures suggested .NET/EF Core seeding pattern (config-driven, never in source)
- (decision by user) FR7/FR13 amended: weekends treated as shop-closed, only weekdays bookable -- user explicitly acknowledged this is unrealistic for real barbershops but wants it for this build
- (decision by user) FR3 amended: role-gating extended to signed-in wrong-role users (not just unauthenticated) -- server-side rejection AND UI/nav hidden entirely for inaccessible pages
- (decision by user) FR34 added (PM suggestion, accepted): system blocks any permission change or account deletion that would leave zero admin accounts
- (decision by user) FR35 added + FR28 amended: admin-driven password change on another account forces that account's ALL sessions to end immediately (forced re-login); self-service password change via own Account page (FR28) does NOT end the user's own session -- addendum captures a session-version-counter pattern to support this given the JWT leaning
- (open item, needs follow-up) NOT yet confirmed: does an admin-driven PERMISSION LEVEL change (not password) also force that account's sessions to end immediately, for the same stale-privilege reason? Asked user, awaiting answer.
- (decision by user) FR8 amended: slots within 30 min of current time excluded when booking for today (30-min advance-notice cutoff)
- (decision by user) FR30/NFR2 amended: cancellation made transactional + idempotent (succeeds exactly once; second attempt on already-cancelled appointment gets clear error); same transactional handling extended to cancel-vs-book races
- (decision by user) Success Metrics: added explanatory note (not a fake metric) that business-outcome/adoption metrics don't apply since the app has no real users/production deployment -- resolves rubric's "metrics validate correctness not thesis" finding without forcing an inapplicable outcome metric
- (decision by user) FR18/FR28 amended: explicit confirm step required for ALL account-information edits (self via Account page, admin via Accounts page), same pattern as cancellation (FR30)
- (decision by user) FR1/FR18 amended: duplicate email rejected with clear error (email is the account's unique key) -- applies to registration and admin email edits alike
- (decision by user) password strength/complexity explicitly added to Out of Scope -- not important for this project
- (decision by user) FR40 added: admin can delete accounts from the same Accounts page (edit/create/delete unified), gated behind confirm step
- (decision by user) FR41 added + NFR2 extended: concurrent account edits/deletes (two admins, or admin-vs-self-edit) handled transactionally the same way as double-booking -- first commit wins, second gets clear conflict error
- (decision by user) FR31 confirmed: addendum strengthened to explicitly state seed admin credentials must never be committed to source (placeholder-only appsettings.json, real values via user-secrets locally / GitHub Actions secret in CI)
- (decision by user) FR34 confirmed as-is, no changes
- (decision by user) Success Metrics: replaced the explanatory footnote (rejected by user as unprofessional / confusing) with an actual metric row -- "new customer goes from home page to confirmed booking in under a minute, no phone/wait" with counter-metric that speed doesn't skip real validation (FR10). This resolves the rubric's "metrics validate correctness not thesis" finding by tying a metric to the original phone-friction problem, framed as an achievable, testable-without-real-users speed/UX metric rather than a real adoption stat
- (decision by user) FR35 amended: admin-driven PERMISSION LEVEL change (distinct from password change) does NOT force session end -- a page refresh is sufficient to pick up the new role, no full re-login required. Addendum notes the design implication (role can't be trusted as long-lived token truth the way password-session-version is; either check role live against DB per request, or use short-lived/refreshed tokens)
- (decision by user) FR24 amended: appointments list bounded to upcoming only; past appointments retained (not deleted) and tagged Finished
- (decision by user) FR7 amended: forward booking window capped to 30 days (~1 month) -- SUPERSEDES the brief addendum's earlier decision ("Options Considered -- Booking Window Cap": a 7-day cap was explicitly dropped as "an arbitrary simplification with no real business justification"). This new cap is NOT arbitrary in the same sense -- it's driven by review-flagged risk (unbounded future accumulation with no shop-closure model, "book year 9999" edge case) rather than being dropped without reason.
- (event) user asked to reconcile the FR7 disagreement -- annotated brief-bmad-learning-project-2026-07-20/addendum.md in place with a "Superseded during PRD" note pointing to this PRD's FR7 as current source of truth; appended a matching event to the brief's own .memlog.md. brief.md itself untouched (didn't state the cap decision directly, only its addendum did); brief status remains final, historical record preserved
- (event) user asked to reconcile the mobile-responsiveness disagreement too -- annotated brief.md's Scope section bullet in place with a "superseded" pointer to this PRD's FR22/NFR3; appended matching event to brief's .memlog.md; original brief bullet text preserved alongside the annotation, brief status remains final
- (event) walked medium/low reviewer-gate findings in detail with user; resolved as follows:
- (decision by user) renamed "Accounts tab"/"Accounts page" -> "Admin Panel" everywhere (FR16, FR18, FR19, FR40, UJ-3) to fix the Accounts-vs-Account naming collision
- (decision by user) standardized booking-surface naming: "Schedule Appointment page" = the page, "Schedule Appointment nav button" = the nav element (FR3); UJ-2's "shared booking page" reworded to match
- (decision by user) Success Metrics table given IDs SM1-SM8
- (decision by user) NFR6 reworked from unfalsifiable prose to a concrete, manually-reviewed proxy (organization by responsibility, no god-classes) per PM suggestion; SM5 reworded to match
- (decision by user) FR10 reworded: dropped "clear error," replaced with concrete behavior (error shown on screen; backend does not create a duplicate appointment; does not crash)
- (decision by user) FR17 amended: admin searches by name OR email, partial match, all matches shown
- (decision by user) FR2 amended: identical generic error ("Invalid email or password") on any failed login regardless of cause -- no user enumeration
- (decision by user) NFR1 amended: added login rate-limiting (threshold deferred to Architecture) and fixed EST timezone as the authoritative clock for all date/time comparisons
- (decision by user) FR19 amended: admin-created accounts also require password typed twice to confirm, consistent with FR1/FR18/FR28
- (decision by user) FR1 amended: mismatched password-confirmation is rejected with a "passwords do not match" error, only the two password fields need retyping, other fields preserved -- stated as applying to every password-confirmation flow in the PRD (FR1, FR18, FR19, FR28)
- (decision by user) FR4 amended: customers land directly on the Schedule Appointment page after sign-in (previously "land normally, navigate via a tab") -- this also resolves the earlier open question about returning to intended page after a CTA-triggered login redirect, since that's now the default landing page anyway
- (decision by user) FR6 amended: explicit "No barbers available" empty state when no barber accounts exist
- (decision by user) FR9 amended: successful submission transitions to a confirmation screen ("Appointment booked with X at Y"), which also naturally prevents accidental duplicate-submission of the same booking; same customer cannot hold two appointments at the same date/time across different barbers (self-double-booking blocked) -- added on PM recommendation, user had asked whether this was necessary
- (decision by user) FR15 amended: admin's Select Barber dropdown auto-selects the first barber by default rather than showing an empty state
- (decision by user) FR18/FR40 amended: demoting OR deleting a barber account cancels and deletes only that barber's FUTURE (not-yet-occurred) appointments; past/Finished appointments are retained as history -- corrected after user confirmed history should be preserved
- (event) Polish pass run in parallel on prd.md and addendum.md (structural pass then prose pass, sequential within each file, per doc_standards): prd.md -- trimmed a redundant Overview bullet, merged Open Items into the Out of Scope guest-booking bullet (no info lost), converted FR group headers to proper ### headings, fixed one run-on list in Overview; addendum.md -- converted two dense paragraphs to bullet lists matching existing schema, front-loaded the "never commit seed credentials to source" rule, split the FR35 role-staleness design implication into its own subsection, two minor word-agreement fixes. All FR1-41/NFR1-7/SM1-8 IDs and every hedged "leaning, not locked"/"deferred to Architecture" phrase preserved verbatim -- no content or decision changed by polish.
- (event) polish complete; user is doing a final personal re-read before close
- (decision by user) Overview goal sentence reworded to drop "calling and waiting on hold" framing (no one calls to book a haircut) -- now "give customers a way to book a time so they don't have to sit and wait during their busy days." SM8 reworded to match (dropped "no phone call ... required"), same reasoning applied proactively for consistency
- (decision by user) global wording sweep: every instance of "a clear error" changed to "an error message" (SM3, FR1, FR10 "error is shown" -> "error message is shown", FR18, FR30, FR34, FR41 "clear conflict error" -> "conflict error message", NFR2)
- (decision by user) MAJOR MODEL CHANGE: single-admin assumption -- admin acts as Owner. Exactly one admin account ever exists (the one seeded per FR31); no account can be promoted to admin, the admin account can never be demoted or deleted, and it is not part of the Admin Panel's searchable/manageable set (FR17). FR18's permission-level options reduced to (customer/barber) only. FR19 now creates barber accounts only, not admin. FR34 rewritten around this single-admin invariant, replacing the old "prevent reaching zero admins" logic. FR41 no longer mentions "two admins editing at once" since only one admin ever exists. UJ-3 and addendum's First-Admin Bootstrap section updated to match.
- (event) user made a small direct edit to the Overview goal sentence themselves (minor wording polish, same intent already agreed -- book a time slot in advance, no phone/wait framing) -- no new decision, just a wording tweak
- (event) user did final personal re-read and approved -- PRD finalized: status set to final, updated to 2026-07-22
- (decision by user) FR23 amended: sign-out ends the session server-side across all tabs/devices for that account, not just the local tab
