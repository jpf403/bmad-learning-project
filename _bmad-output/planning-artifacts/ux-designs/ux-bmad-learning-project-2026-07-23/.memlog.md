---
topic: Barbershop Appointment Scheduler — UX
updated: 2026-07-27T00:00
---

- (event) UX workflow started; python/uv unavailable in this environment, memlog maintained by hand in script-equivalent format
- (decision by user) source input confirmed: prd-bmad-learning-project-2026-07-21 (prd.md, addendum.md) — brief.md/brief addendum not used as direct UX sources
- (event) subagent dispatched to extract UX-relevant facts (roles, journeys, screens, nav rules, interaction rules, platform constraints, out-of-scope, open items, brand/tone) from prd.md + addendum.md
- (note) extraction complete: 3 roles (customer/barber/single-admin), 4 UJs, 11 implied surfaces (Home, Register, Login, Schedule Appointment, Barber Dashboard, Admin Schedule view, Admin Panel, Account page, About, post-booking confirmation, cancel-confirm step), heavy confirm-before-action + transactional/idempotent rules throughout, mobile+desktop responsive required with hover-vs-touch single-tap constraint (FR20/FR22/NFR3), PRD carries zero brand/tone/visual guidance -- UX originates identity from scratch
- (decision by user) working mode: coaching path
- (note) form-factor already resolved from PRD extraction: responsive web, mobile + desktop viewports (FR22/NFR3), no native app
- (decision by user) stakes: normal/consumer-grade rigor -- explicit reason: user wants to learn to use the method effectively, not a lightweight pass
- (decision by user) overall visual direction: slick modern app, white background, blue as primary interactive color (buttons etc.); exact hue/shade/type not yet chosen
- (decision by user) brand name: "Fake Barbershop" -- placeholder wordmark logo, no graphic mark needed, simple text is fine
- (decision by user) top nav bar (all pages): logo left, then Home / Schedule Appointment / About / My Schedule / Admin Panel links -- My Schedule hidden unless barber or admin permission, Admin Panel hidden unless admin permission (matches FR3 role-hiding)
- (decision by user) nav right side: signed-out shows Sign In + Register buttons; signed-in shows profile icon opening a dropdown (per FR29, already in PRD)
- (decision by user) Home page layout: curved diagonal line splits page into blue (right) and white (left) halves; blue side has a scissor + comb graphic; white side has headline copy + a blue "Schedule Appointment" CTA button
- (decision by user) Schedule Appointment page: white background, two-part vertical layout -- top is the booking form (barber select, date, time) with Submit at the bottom of that section; bottom section is "My Appointments" showing the signed-in user's future bookings (matches FR24)
- (decision by user) My Schedule page (barber + admin): mostly text -- Date header with two arrows either side to change day, then descending list of 30-min time slots (per PRD); booked slots show customer name + a red Cancel button; admin view adds a white barber-select dropdown with a subtle box-shadow, positioned next to the date header
- (decision by user) button hover state: all buttons get a darker-hue hover treatment on desktop/pointer devices only -- explicitly NOT on mobile/touch (ties to FR20/NFR3 hover-vs-touch concern)
- (decision by user) Admin Panel: search bar at top; matching accounts render as simple clickable bars; clicking opens a popup with that account's editable info, with Submit (save) or Delete actions inside the popup
- (decision by user) confirm-action pattern (applies everywhere a confirm step is required, e.g. cancellations, account edits, deletes): popup with two buttons -- "Go Back" in white, "Confirm" in the same red used for Cancel buttons
- (decision by user) confirm-popup pattern refined: red "Confirm" reserved for destructive actions only (cancel appointment, delete account); non-destructive confirms (e.g. saving profile/account edits) use a neutral/blue Confirm button instead. "Go Back" stays white for all.
- (decision by user) shape language: sharp corners (not rounded) for a "slick modern with character" feel -- explicitly exploratory, user knows this can be revisited later
- (event) dispatching color-themes creative tool subagent to render 4-6 blue/white/sharp-corner palette+type directions against the actual home page concept (Fake Barbershop, curved blue/white split, scissor+comb, Schedule Appointment CTA) for user to pick from
- (event) color-themes-1.html rendered to .working/ with 6 variations: Corporate Trust (#1B3A6B, Inter), Startup Fresh (#2E5AEB, Poppins/Inter), Barbershop Classic Meets Modern (#3E5C76, IBM Plex Sans), Precision Tech (#4A47C4, Space Grotesk/IBM Plex Sans), Modern Warmth (#0E7C9B, Manrope), Bold Editorial (#1447E6, Sora/Work Sans) -- awaiting user pick
- (decision by user) picked color/type direction: "Modern Warmth" -- primary #0E7C9B, primary-fg #FFFFFF, hover #0B5F79, destructive red #D64545 (destructive-only), bg #FFFFFF, neutral/muted #EFF6F8, border #D3E4E9, font Manrope, corners fully square (border-radius:0, no clip-path) -- from color-themes-1.html variation 5
- (decision by user) frontend build approach: fully custom CSS/components, EXCEPT use unstyled accessible Radix UI primitives (MIT license) specifically for calendar/date-picker, dropdowns, and modal/dialog -- the three highest-risk spots for accessibility bugs (focus-trapping, keyboard nav, ARIA semantics) if built fully from scratch. Everything else (buttons, nav, forms, layout) fully custom.
- (decision by user) elevation model confirmed: border token (#D3E4E9) for elements in normal page flow (schedule rows, admin account bars, form sections); subtle box-shadow reserved only for elements floating above the page (confirm popups, account-edit popup, open dropdown menus) -- shadow consistently means "floating," not decoration
- (decision by user) dark mode: light-only, no dark theme variant
- (decision by user) motion: minimal -- fast, subtle fades/slides on popups and dropdowns only, nothing decorative/elaborate
- (decision by user) voice/tone: clean and professional -- neutral, plain-spoken microcopy, no jokes/exclamation points
- (decision by user) accessibility target: WCAG 2.2 AA baseline, consistent with "normal rigor" stakes call and the Radix-primitive choice
- (event) Key Flow 1 narrated by user, named protagonist "Jack": signed-out on Home -> Login -> lands on Schedule Appointment (signed-in customer default) -> selects barber Manny -> calendar picks July 24 (tomorrow) -> time dropdown 11:00 AM -> Submit -> confirmation screen ("Appointment ... set for Jack [lastname] with Manny on July 24, 11:00 am" -- user flagged this exact wording as rough draft, needs polish) -> closes tab with certainty booking exists
- (note) confirmation copy to be tightened toward clean/professional voice per FR9 pattern: "Appointment booked with Manny at 11:00 AM on July 24." -- proposed to user for the polished spine wording
- (event) drafting named-protagonist Key Flow 2 (Manny, barber, daily schedule check) and Key Flow 3 (Owner/admin, account management) from PRD UJ-2/UJ-3 + already-locked page designs, to present to user for correction rather than re-eliciting from scratch
- (decision by user) confirmation copy for Flow 1 approved: "Appointment booked with Manny at 11:00 AM on July 24."
- (decision by user) Flow 2 correction: barbers do NOT book walk-in customers through the app (remove that beat) -- climax reworked around a new online booking appearing on the schedule mid-day instead
- (decision by user) Flow 3 correction: editing account fields (not deletion) is the more common/primary case for the admin popup -- both edit and delete use the identical popup UI, differing only in the Confirm button color already decided (blue for non-destructive edit, red for destructive delete)
- (decision by user) About page content: fake shop location + phone number, plus a list of barbers (matches FR21 "static content")
- (decision by user) scissor + comb graphic on Home hero should be crossed like an X
- (decision by user) Login and Create Account (Register) pages follow the same visual/interaction principles as every other page (Modern Warmth palette, Manrope, sharp corners, same form/button conventions) -- no bespoke treatment
- (event) Discovery closed -- user confirmed nothing else to add; moving to Finalize
- (event) DESIGN.md distilled (3329 words, full frontmatter + all 8 canonical body sections) from memlog + color-themes-1.html + PRD sources
- (open item, needs follow-up) destructive red #D64545 on white computes to ~4.38:1 contrast, just under WCAG 2.2 AA's 4.5:1 threshold for normal-size text (clears 3:1 for large text/non-text) -- destructive button labels are 14px/600, a real narrow shortfall; needs client decision: accept as-is, or darken the destructive fill slightly for text-bearing contexts
- (open item, needs follow-up) no error/warning color exists in the locked palette; double-entry password mismatch state needs a treatment that isn't the destructive red (that's reserved for destructive actions only) -- doc used a plain-text fallback pending client input
- (note) DESIGN.md proposed several unreviewed defaults (flagged in-doc, not hidden): destructive-hover #AB3737, text/text-muted colors (#17242A/#5B7480), full typography size ramp beyond the Manrope family choice, 4px-base spacing scale + breakpoints (640/1024px), Sign In (secondary)/Register (primary) button variants, signed-in profile icon glyph left open, home hero headline copy explicitly deferred to EXPERIENCE.md/content pass
- (event) EXPERIENCE.md distilled (4086 words, Foundation/IA/Voice+Tone/Component Patterns/State Patterns/Interaction Primitives/Accessibility Floor/Responsive & Platform/Key Flows) from memlog + DESIGN.md tokens + prd.md/addendum.md, citing FR/UJ numbers verbatim
- (note) EXPERIENCE.md flagged its own defaults: cold-load "Loading..." placeholder, one-level popup-stacking interpretation (edit popup -> confirm popup on top, single level), several unspecified microcopy strings (empty appointments, no search results, pre-search prompt, concurrent-edit conflict), no-return-to-originally-requested-page after signed-out redirect, wrong-role direct-URL access redirects quietly to own landing page, nav-collapse breakpoint behavior, Enter-to-submit on single-action forms
- (event) reconcile-prd.md run: spines checked against every FR/NFR/UJ in prd.md/addendum.md -- 2 real gaps + 1 contradiction found, all fixed directly in EXPERIENCE.md: (1) added duplicate-email error state (FR1/FR18/FR19), (2) added login rate-limiting lockout state (NFR1, threshold left unspecified per Architecture deferral), (3) split the single admin edit/create popup row into separate Edit (has customer/barber permission dropdown) and Create (no permission selector, always creates a barber account per FR19) rows -- Create previously incorrectly implied a permission dropdown
- (decision by user) destructive red contrast fix: darken the shade rather than accept the AA shortfall -- destructive #D64545 -> #C93A3A (~5.06:1 on white, was ~4.38:1), destructive-hover #AB3737 -> #A83030 (~6.71:1) -- applied to DESIGN.md tokens/prose and EXPERIENCE.md's Accessibility Floor note
- (decision by user) no dedicated error/warning color added to the palette -- password-mismatch and similar validation states stay plain text, keeping red reserved strictly for destructive actions -- confirmed as final, not a pending gap
- (decision by user) Reviewer Gate: run the rubric walker only (skip additional accessibility-lens reviewer)
- (event) dispatching rubric walker subagent per references/validate.md to review DESIGN.md + EXPERIENCE.md as a downstream-consumer contract
- (event) review-rubric.md complete: overall verdict strong, 0 critical/high, 5 medium + 5 low findings, all 10 fixed directly (no new user decisions needed): text-muted contrast ratio stated, date-nav-arrow + admin-account-popup components added to DESIGN.md, My Appointments rows explicitly reuse schedule-row-booked, Home hero naming aligned across both files, Registration-success and Account-save-success states added, My Schedule cold-load state added, Foundation prose trimmed of persuasive framing, Flow 4 (sign-out) stub added for UJ-4 completeness
- (decision by user) key-screen mocks to render: Home page, Schedule Appointment, My Schedule (dashboard view), Confirm-action popup
- (event) 4 key-screen mocks rendered to .working/: key-home.html, key-schedule-appointment.html (incl. confirmation-screen state), key-my-schedule.html (admin variant), key-confirm-popup.html (destructive + non-destructive variants side by side)
- (decision by user) mock coverage confirmed sufficient at 4 screens; Login, Register, Admin Panel, Account, About stay spine-only (no additional mocks requested)
- (decision by user) footer added to scope, present on every page (like nav bar): "Fake Barbershop" wordmark, address + phone (same fake info as About page), hours "Mon-Fri, 9:00 AM - 4:30 PM" (ties to real booking hours + weekends-closed rule), copyright line "(c) 2026 Fake Barbershop". Visual: {colors.neutral} background + {colors.border} top border (in-flow, no shadow), muted text -- reuses the "subtle section separation" use of neutral already defined in DESIGN.md rather than introducing anything new
- (event) footer added to DESIGN.md (frontmatter component + Components/Elevation prose) and EXPERIENCE.md (IA note + Component Patterns row); retrofitted into the 3 full-page key-screen mocks (key-home, key-schedule-appointment both states, key-my-schedule) -- key-confirm-popup unchanged since it's a popup-only mock, not a full page
- (event) 4 key-screen mocks promoted from .working/ to mockups/ (home.html, schedule-appointment.html, my-schedule.html, confirm-popup.html) -- originals also left in .working/ as audit trail; inline `mockups/*.html` links added at the relevant DESIGN.md bullets (Home hero, Confirmation screen, Elevation named exception, Confirm-action popup) and EXPERIENCE.md's Information Architecture section, with spines-win-on-conflict stated once
- (event) structural editorial pass (doc_standards step 1) applied to both spines: condensed 3 near-verbatim repeated passages down to pointers (DESIGN.md's square-corner rationale restated in both Brand & Style and Shapes; EXPERIENCE.md's nav-hiding DOM-removal detail restated in both Information Architecture and Component Patterns; EXPERIENCE.md's no-error-color reasoning restated in both State Patterns and Accessibility Floor) -- no decisions/tokens/citations changed, content-sacrosanct
- (event) prose editorial pass (doc_standards step 2) applied: fixed an unclear phrase in DESIGN.md's text-muted contrast note ("darker-of-white or lighter-of-hue" -> "get lighter or shift hue"), and replaced an ALL-CAPS emphasis ("NOT auto-signed-in") with italics in EXPERIENCE.md's Registration-success state for consistency with the doc's own accessibility stance and existing emphasis conventions. No content/decisions changed.
- (event) FINALIZE COMPLETE: DESIGN.md and EXPERIENCE.md both set to status: final, updated: 2026-07-23. Deliverables: DESIGN.md, EXPERIENCE.md, 4 promoted mockups (mockups/home.html, schedule-appointment.html, my-schedule.html, confirm-popup.html), reconcile-prd.md, review-rubric.md, full .memlog.md decision trail. Common next steps: bmad-architecture, bmad-create-epics-and-stories.
- (event) UPDATE MODE opened 2026-07-24 -- user received outside feedback (source unspecified, likely a reviewer/boss) on the finalized UX
- (note) feedback received: (1) Home page is fine as-is; (2) other pages "lacked color" -- implies too monochrome/white beyond Home; (3) wants a more "Figma-based style, like rounded buttons and more" -- directly reverses the earlier explicit "fully square corners" decision from Discovery (which was flagged at the time as exploratory/revisitable)
- (decision by user) corner radius: subtle rounding (~6-8px) on buttons/inputs/cards -- reverses the "fully square, 0px everywhere" decision
- (decision by user) color-usage treatment on non-Home pages: undecided, wants to see mocked options before choosing -- dispatching a color-usage direction render on Schedule Appointment (representative complex page) with the new subtle rounding applied
- (event) color-usage-directions-1.html rendered to .working/ with 4 variations on Schedule Appointment (all using only existing tokens + 8px rounding): Tinted sections, Colored accents, Bolder header band, Combined -- awaiting user pick
- (decision by user) color-usage direction picked: "Tinted sections" -- form cards and rows get a light neutral-wash fill instead of white+border, no new colors introduced
- (conflict flagged) user requested Finished appointments be removed entirely from My Appointments, believing this was already specified -- this CONTRADICTS the finalized PRD's FR24 ("past appointments retained, not deleted, tagged Finished"), a deliberate decision made during the PRD run (2026-07-22), not a UX-only assumption -- surfaced to user for clarification rather than silently implemented
- (decision by user) update the PRD itself (not just the UX) -- FR24 amended in prd.md: past appointments retained in DB but no longer shown/tagged in the Schedule Appointment list at all. prd.md status stays final, updated bumped to 2026-07-24, change logged in the PRD's own .memlog.md
- (event) EXPERIENCE.md updated to match: My Appointments list row (Component Patterns), removed "Finished appointments" State Patterns row entirely, scrubbed "Finished" references from Accessibility Floor's no-color-alone bullet and Interaction Primitives' Banned-everywhere list, reworded barber-demotion/deletion-cascade row to say past appointments are retained in DB but never surfaced in any customer-facing list
- (decision by user) hold off on a secondary accent color for now -- no clear job identified for one yet, palette stays primary teal + destructive red only
- (decision by user) apply rounding + tinted-sections changes as new mockups, keeping the same layouts as before -- Home page unchanged (already approved), non-Home pages get subtle 6-8px rounding + neutral-wash tinted rows/cards (no border) in place of white+border
- (event) DESIGN.md revised in place: rounded scale sm4/DEFAULT6/md6/lg8/xl8/full9999 (cascades to all components via token refs, no per-component edits needed for radius), schedule-row-open/booked + admin-account-row switched to neutral-fill/no-border/border-hover, new form-section component added (neutral fill, rounded.lg, used by booking form + Account/Login/Register), Brand & Style/Colors(Neutral+Border bullets)/Shapes/Elevation & Depth/Components/Do's-Don'ts prose all updated to match, honest note added that this reverses the earlier all-square/border-only decision. EXPERIENCE.md touched up: nav-collapse's stale "square" reference removed, Booking form + Account page form rows now cite {components.form-section}. Both files' updated bumped to 2026-07-24, status stays final.
- (event) regenerating the 3 non-Home mockups (schedule-appointment, my-schedule, confirm-popup) with new tokens, same layouts, Home mockup left untouched per user approval
- (event) mockups/ updated in place, same layouts throughout: schedule-appointment.html -- form-section + appt-rows now neutral-tinted/no-border/6-8px radius, second (Finished) appointment row removed entirely from markup; my-schedule.html -- slot rows switched from joined-border list to individually tinted/rounded rows with a small gap (admin barber-select keeps its shadow-at-rest exception, now with rounded corners too); confirm-popup.html -- popup stays white/floating (shadow unchanged) but corners rounded to 8px, buttons to 6px. home.html -- left as-is per user approval except button corner-radius updated to match the new global token (consistency across the whole product), no color/layout changes. .working/ historical copies intentionally left unchanged as the audit trail of the original Finalize-time render.
- (event) UPDATE MODE opened 2026-07-27 -- 2026-07-27 implementation readiness report (John, PM agent) flagged two UX-owned open items as blockers for Story 1.1 (UX-DR20): no validation-error color, unsized tablet breakpoint. Both had been informally touched during Discovery/prior Update passes but never actually closed in the documents themselves.
- (event) rendered .working/error-color-options.html -- 4 candidate validation-error hues shown in context (duplicate-email message, password-mismatch caption) against the locked palette, each with WCAG contrast math against white: Amber #B45309 (~5.02:1), Deep Amber #92400E (~7.09:1), Orange #C2410C (~5.18:1), Gold #A16207 (~4.93:1)
- (decision by user) validation-error color: reuse the destructive red directly rather than any of the 4 rendered options -- implemented as a new, semantically distinct `{colors.error}` token sharing `{colors.destructive}`'s hex (#C93A3A) so a future re-tune of one doesn't silently drag the other; `{colors.error}` is for validation-message text only, never a button/fill -- `{colors.destructive}` stays reserved for actual destructive actions (Cancel, Delete). Resolves the open gap flagged at the original 2026-07-23 Finalize.
- (decision by user) breakpoints locked as final: 640px / 1024px (mobile / tablet / desktop split) -- user confirmed after an explanation of what the breakpoints control; no change from the values already drafted throughout both spines and the mockups, just converting them from placeholder to a real decision.
- (event) DESIGN.md and EXPERIENCE.md updated in place to close both UX-DR20 items: DESIGN.md gets the `{colors.error}` token (frontmatter + Colors/Components/Do's-and-Don'ts prose) and the breakpoint placeholder language removed from Layout & Spacing; EXPERIENCE.md's State Patterns (password mismatch, duplicate email), Accessibility Floor, and Responsive & Platform breakpoint table updated to match. No mockups affected -- password-mismatch/duplicate-email states live on Register/Account/Admin surfaces, which stayed spine-only (no rendered mocks) per the original Finalize's mock-coverage decision. Both files' `updated` bumped to 2026-07-27, `status` stays `final`.
- (decision by user) Reviewer Gate: skip validation for this pass -- two narrow, well-scoped decisions with no ripple into other sections or mockups.
- (event) UPDATE FINALIZE COMPLETE 2026-07-27 -- both UX-DR20 items (validation-error color, tablet breakpoint) closed for real, not just cross-document agreement that they're open. Unblocks Story 1.1 per the 2026-07-27 implementation readiness report.
