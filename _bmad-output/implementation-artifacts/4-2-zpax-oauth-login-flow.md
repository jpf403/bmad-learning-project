---
baseline_commit: b097454dae3ef2ec1f74f4927b6faa526b963c63
---

# Story 4.2: z-pax OAuth Login Flow

Status: done

## Story

As a visitor or existing user,
I want to sign in via z-pax SSO,
so that I can access my account without creating a separate password.

## Acceptance Criteria

1. **Given** the Login page, **when** a user clicks "Sign in with z-pax", **then** the backend generates a random `state` value, stores it in a short-lived cookie, and redirects the browser to z-pax's authorization endpoint (`https://sapi.auth.myzpax.com/connect/authorize`) with `client_id`, `scope=api`, `response_type=code`, `redirect_uri`, `fallback_uri`, and `state` (AD-19, FR42). **Updated 2026-08-24** against z-pax's real `/connect/authorize` API reference: `scope=api` replaces this AC's original `offline_access profile` guess (z-pax's doc lists `scope`'s value the same single-word way it lists `response_type=code` — a literal required value, not free-form OAuth scopes) and `fallback_uri` (required per that same reference, undocumented purpose) is now sent, set to the same `https://localhost:5173/login?error=sso_failed` target as this story's own failure redirect (Task 4) as a reasonable default for "no real z-pax credentials to trial against yet." **Pending live verification once real z-pax credentials are available** — confirm `scope=api` alone still grants `/connect/userinfo` access, and observe what z-pax actually does with `fallback_uri` in practice.
2. **Given** a successful authorization, **when** z-pax redirects back to `/api/auth/sso/callback` with a `code` and `state`, **then** the backend first validates `state` matches the stored value — rejecting the callback (redirect to Login with an error, no account created or session issued) on a missing or mismatched `state` — then exchanges `code` at `https://sapi.auth.myzpax.com/connect/token` for a z-pax access token, calls `GET https://sapi.auth.myzpax.com/connect/userinfo` with that access token to fetch the user's `id`/`email`/`firstName`/`lastName`, and resolves the local account via `CreateOrLinkSsoAccount` from Story 4.1 (FR43, FR44, AD-19).
3. **Given** the resolved account, **when** the SSO flow completes, **then** the app mints its own access token (in-memory) and refresh token (HttpOnly cookie) exactly as `POST /api/auth/login` does, and routes the user per FR4 (AD-3, AD-19).
4. **Given** automated tests should never depend on a live external service (mirrors AD-4), **when** this story is implemented, **then** the OAuth calls are built against an injected `ISsoClient` abstraction with a fake test double for xUnit coverage, while the real `ISsoClient` implementation targets z-pax's actual endpoints (AD-19) using the real Client ID/Secret expected to be available by implementation time.
5. **Given** z-pax returns an error or the callback is missing/invalid `code`, **when** this happens, **then** the user is redirected to Login with an on-screen error, and no account is created or session issued.

## Tasks / Subtasks

- [x] **Task 1: `ZPaxSsoOptions` + non-secret config, env-var-only secrets** (AC: #1, #2)
  - [x] `Services/ZPaxSsoOptions.cs`: `AuthorizationEndpoint`, `TokenEndpoint`, `UserInfoEndpoint`, `RedirectUri` (all `string`, non-secret), `ClientId`, `ClientSecret` (`string`, secret — populated only from env vars, never given a value in `appsettings.json`). Mirror `JwtOptions.cs`'s shape exactly. **Updated 2026-08-24:** `UserInfoEndpoint` added for consistency with the other two z-pax endpoint URLs — it was initially hardcoded in `ZPaxSsoClient.cs` instead, which had no principled justification (Jack asked "how come the userinfo endpoint isn't configurable too?" and there wasn't a good answer).
  - [x] `Program.cs`: `builder.Services.Configure<ZPaxSsoOptions>(builder.Configuration.GetSection("ZPaxSso"));`
  - [x] `appsettings.json`: add a `ZPaxSso` section with **only** the non-secret keys populated: `"AuthorizationEndpoint": "https://sapi.auth.myzpax.com/connect/authorize"`, `"TokenEndpoint": "https://sapi.auth.myzpax.com/connect/token"`, `"UserInfoEndpoint": "https://sapi.auth.myzpax.com/connect/userinfo"`, `"RedirectUri": "https://localhost:7113/api/auth/sso/callback"`. Do **not** add `ClientId`/`ClientSecret` keys here at all — same convention as `AdminSeed` having zero footprint in `appsettings.json` (AD-6), not even an empty placeholder.
  - [x] `Properties/launchSettings.json` (gitignored, local secrets): add `"ZPaxSso__ClientId": ""` and `"ZPaxSso__ClientSecret": ""` empty placeholders to both the `http` and `https` profiles' `environmentVariables`, exactly mirroring `AdminSeed__Email`/`AdminSeed__Password`'s existing placeholder pattern. Jack fills in the real values locally once available — never commit real secrets.
  - [x] `Properties/launchSettings.example.json` (the committed template a fresh clone actually sees): add the same two keys to both profiles too, using the same `"replace-with-..."`-style placeholder text `AdminSeed__Email`/`AdminSeed__Password` already use there — every existing env var appears in both files today, and skipping this one would break that parity.

- [x] **Task 2: `ISsoClient` abstraction, real implementation, fake test double** (AC: #2, #4, #5)
  - [x] `Services/ISsoClient.cs`: `string BuildAuthorizationUrl(string state)` and `Task<SsoIdentity> ExchangeCodeForIdentity(string code)`. Add `public record SsoIdentity(string Email, string FirstName, string LastName, string SubjectId)` in the same file.
  - [x] `BuildAuthorizationUrl` builds the full authorize URL from `ZPaxSsoOptions` (`AuthorizationEndpoint` + `client_id`, `scope=api`, `response_type=code`, `redirect_uri`, `fallback_uri`, `state` query params) — use `Microsoft.AspNetCore.WebUtilities.QueryHelpers.AddQueryString` for correct URL-encoding, not manual string concatenation. `scope`/`fallback_uri` updated 2026-08-24 per z-pax's real `/connect/authorize` reference (see AC #1's update note); both remain hardcoded constants in `ZPaxSsoClient.cs` (not config) since they're fixed values this app always sends, unlike the three endpoint URLs.
  - [x] `Services/ZPaxSsoClient.cs` implements `ISsoClient` against the real endpoints via an injected `HttpClient` (no OAuth NuGet package, per the Stack table). `ExchangeCodeForIdentity` is a two-call sequence — z-pax's confirmed real contract, not a guess:
    1. POST form-encoded `grant_type=authorization_code`, `code`, `redirect_uri` (**must** match the exact value sent to the authorize endpoint — a mismatch is a common real-provider rejection), `client_id`, `client_secret` to `TokenEndpoint`; parse the JSON response for its `access_token`.
    2. GET `ZPaxSsoOptions.UserInfoEndpoint` (`https://sapi.auth.myzpax.com/connect/userinfo`) with header `Authorization: Bearer {access_token}` and `Accept: application/json`. Response body: `{ id: int32, email: string|null, firstName: string|null, lastName: string|null, phoneNo, emailVerified, hasMetadata, isLocked, avatar, createdAt }`. Deserialize with `JsonSerializerOptions { PropertyNameCaseInsensitive = true }` — the response uses camelCase field names (`firstName`, `lastName`, `accessToken` on the token response) which won't bind to PascalCase C# properties without this (or explicit `[JsonPropertyName]` attributes). **Updated 2026-08-24:** `UserInfoEndpoint` moved from a hardcoded constant into `ZPaxSsoOptions`/`appsettings.json`, alongside `AuthorizationEndpoint`/`TokenEndpoint`, for consistency — all three are z-pax endpoint URLs on the same host and should be configured the same way; there was no principled reason for the original asymmetry (Task 1 simply hadn't listed it).
    3. Map to `SsoIdentity(Email: email, FirstName: firstName, LastName: lastName, SubjectId: id.ToString())` — `SubjectId` is the numeric `id` field, stringified (`ISsoClient.SubjectId` stays `string` per its existing signature; the userinfo endpoint's `id` is `int32`).
    4. `email`, `firstName`, and `lastName` are all nullable per z-pax's contract; if any comes back `null`, throw — the Controller's existing `try`/`catch` (Task 4) already maps any exception from this method to the same generic failure redirect as AC #5, and `CreateOrLinkSsoAccount` requires non-null values for all three.
  - [x] Add an internal DTO (e.g. `ZPaxUserInfoResponse`) in `ZPaxSsoClient.cs` for the `/connect/userinfo` JSON shape above — keep it separate from the public `SsoIdentity` record so z-pax's wire format doesn't leak into the `ISsoClient` abstraction.
  - [x] `Services/ZPaxSsoClient.cs` registration: `builder.Services.AddHttpClient<ISsoClient, ZPaxSsoClient>();` in `Program.cs` (typed client — part of the shared ASP.NET Core framework, no new `PackageReference` needed).
  - [x] `Services/SsoProviders.cs`: `public static class SsoProviders { public const string ZPax = "zpax"; }` — one shared constant referenced by `AuthController`/`AuthService`, same "no ad-hoc string literal" discipline `Role` already established (AD-2). `AccountRepository.CreateOrLinkSsoAccount`/`FindBySsoIdentity` already take `provider` as a plain parameter (Story 4.1) — no repository change needed, just don't hand-type `"zpax"` at each call site.
  - [x] `backend/BarbershopApi.Tests/TestOnly/FakeSsoClient.cs`: implements `ISsoClient`. Mutable public state for tests to configure before each request: `public SsoIdentity NextIdentity` (default a valid identity), `public Exception? ThrowOnExchange` (when set, `ExchangeCodeForIdentity` throws it — simulates "z-pax returns an error", AC #5). `BuildAuthorizationUrl` returns a deterministic fake URL that still echoes `state` (e.g. `$"https://fake-zpax.test/authorize?state={Uri.EscapeDataString(state)}"`) so the login-redirect test can assert against it without hitting a real endpoint.

- [x] **Task 3: `AuthService` — SSO login path reusing existing token-minting** (AC: #2, #3)
  - [x] `IAuthService.cs`: add `Task<(Account Account, string AccessToken, string RefreshToken)> LoginViaSso(string email, string firstName, string lastName, string subjectId)` — same return shape as `Login`.
  - [x] `AuthService.cs`: implement by calling `accountRepository.CreateOrLinkSsoAccount(email, firstName, lastName, SsoProviders.ZPax, subjectId)`, then reuse the **existing private** `GenerateAccessToken`/`GenerateRefreshToken` methods already used by `Login` — do not duplicate token-minting logic. This is the literal mechanism behind AC #3's "mints its own access/refresh tokens exactly as `POST /api/auth/login` does."
  - [x] Let `AdminAccountProtectedException` and `SsoIdentityConflictException` (both already defined, from `CreateOrLinkSsoAccount`) propagate uncaught from `LoginViaSso` — the Controller (Task 4) maps them to the same generic failure redirect as AC #5, since a real z-pax email colliding with the admin account or a genuine unique-index race are both edge cases indistinguishable from "SSO sign-in failed" to the end user.
  - [x] **Do not** call `IAccountRepository.FindBySsoIdentity` anywhere in this story's flow. Story 4.1's own Dev Notes speculated the callback "will likely call it first" for a repeat-login check, but `CreateOrLinkSsoAccount`'s email-keyed branch (verified by reading the current `AccountRepository.cs`) already handles a repeat SSO login correctly and idempotently — it just re-sets the same already-set `SsoProvider`/`SsoSubjectId` on the existing account and returns it. Calling `FindBySsoIdentity` first would add a redundant lookup with no behavioral difference. `FindBySsoIdentity` remains available for a future need but has no caller yet.

- [x] **Task 4: `AuthController` — `GET /api/auth/sso/login` and `GET /api/auth/sso/callback`** (AC: #1, #2, #3, #5)
  - [x] `sso/login`: generate `state` via `Convert.ToBase64String(RandomNumberGenerator.GetBytes(32))` (URL-safe: replace `+`/`/`/`=` or use `Base64UrlTextEncoder`), store it in a cookie named `ssoState` — **`SameSite=Lax`, not `Strict`.** This is the one genuinely non-obvious wire-format detail in this story: `Strict` cookies (used for `refreshToken`, correctly, since that's only ever sent by same-site `fetch`) are **not** sent by the browser on a cross-site top-level navigation — which is exactly what z-pax's redirect back to `/api/auth/sso/callback` is. Copying `refreshToken`'s `SameSite=Strict` here would make the `state` cookie silently vanish on every real callback, permanently 100%-failing AC #2's validation with no code bug to point at. Also `HttpOnly=true`, `Secure=true`, short `Expires` (10 minutes). Build the authorize URL via `ISsoClient.BuildAuthorizationUrl(state)` and `return Redirect(url)`.
  - [x] `sso/callback(string? code, string? state)`: read the `ssoState` cookie, then delete it immediately (`Response.Cookies.Delete("ssoState")`) regardless of outcome — single-use. If the cookie is missing, `state` query param is missing, or they don't match (use a constant-time-safe check is unnecessary here — this isn't a secret comparison against a stored hash, just an equality check against a value that was itself just handed to the same browser) → redirect to the failure target (below). If `code` is missing → same failure redirect (AC #5).
  - [x] On validated state + present code: call `ISsoClient.ExchangeCodeForIdentity(code)` inside a `try`; any exception (including `FakeSsoClient`'s `ThrowOnExchange` in tests) → same failure redirect, no account created (AC #5). On success, call `authService.LoginViaSso(identity.Email, identity.FirstName, identity.LastName, identity.SubjectId)`; catch `AdminAccountProtectedException`/`SsoIdentityConflictException` → same failure redirect (Task 3).
  - [x] On full success: set the `refreshToken` cookie with **identical** `CookieOptions` to `Login`'s (`HttpOnly=true, Secure=true, SameSite=Strict, Expires=+15 days`) — copy, don't re-derive. Redirect (302) to the frontend's role-appropriate landing route, mirroring `frontend/src/landingRoutes.js`'s `LANDING_ROUTE` mapping exactly: `Customer` → `https://localhost:5173/schedule-appointment`, `Barber`/`Admin` → `https://localhost:5173/my-schedule`. Hardcode the `https://localhost:5173` origin — same established convention as `Program.cs`'s CORS policy already hardcoding this exact origin (NFR7 is local-only; AD-19 already fixes the redirect URI the same way for the same reason). Do not introduce a new config key for this.
  - [x] Failure redirect target (used by every rejection path above): `https://localhost:5173/login?error=sso_failed`. This exact query contract (`error=sso_failed`, no more specific detail) is what Story 4.3 will read to render its error message via "the existing login-error display pattern" — deliberately generic, matching this codebase's existing no-detail-leak convention (FR2's identical invalid-credentials message for any failure reason). **Story 4.3 depends on this exact param name — do not invent a different one without updating this note.**

- [x] **Task 5: Tests** (AC: #1–#5)
  - [x] `SqliteApiFactory.cs`: in `ConfigureWebHost`'s `ConfigureServices`, `services.RemoveAll<ISsoClient>();` then register `FakeSsoClient` as a singleton and expose it as `ISsoClient` from the same instance, so tests can reach it via `_factory.Services.GetRequiredService<FakeSsoClient>()` and mutate `NextIdentity`/`ThrowOnExchange` before issuing a request.
  - [x] Tests live in `AuthControllerTests.cs` (same file as `Login`/`Register`/`Refresh` — one Controller, one test file, per this codebase's established convention; no new `SsoAuthControllerTests.cs`).
  - [x] Build the test client via `_factory.CreateClient(new WebApplicationFactoryClientOptions { AllowAutoRedirect = false, BaseAddress = new Uri("https://localhost") })` so redirect responses can be asserted on directly instead of silently followed. **Both settings are required together:** `AllowAutoRedirect = false` alone leaves `BaseAddress` at its `http://localhost` default, and `Program.cs`'s `UseHttpsRedirection()` would then 307-redirect every request before it reaches the SSO endpoints at all — with redirect-following off, the test would observe that 307, not the real response. The existing no-arg `_factory.CreateClient()` override already sets the `https://localhost` base for this same Secure-cookie reason, but it only *hides* the no-arg overload — calling `_factory.CreateClient(options)` goes through the un-hidden base method and needs `BaseAddress` passed explicitly again.
  - [x] `SsoLogin_redirects_to_authorize_endpoint_with_state_cookie` — `GET /api/auth/sso/login`; assert 302, `Location` starts with `FakeSsoClient`'s authorize URL and contains a `state` value; assert a `Set-Cookie: ssoState=...` header is present with `HttpOnly`.
  - [x] `SsoCallback_with_valid_code_and_state_creates_new_customer_account_and_redirects_to_schedule_appointment` — call `sso/login` first to obtain the `state` + cookie (don't hand-craft a state value), then call `sso/callback?code=anything&state={state}` with the `ssoState` cookie attached; assert 302 to `.../schedule-appointment`; assert `Set-Cookie: refreshToken=...`; load a fresh `DbContext`, assert a new `Role.Customer` account exists with `PasswordHash == null` and `SsoProvider == "zpax"`.
  - [x] `SsoCallback_with_valid_code_links_to_existing_barber_account_by_email_preserving_role_and_password` — pre-seed a `Role.Barber` account with `FakeSsoClient.NextIdentity`'s email via the repository directly, run the full login→callback flow, assert 302 to `.../my-schedule`, and assert (fresh `DbContext`) the account's `Role` is still `Barber` and `PasswordHash` is unchanged from the seeded value.
  - [x] `SsoCallback_missing_state_cookie_redirects_to_login_with_error_and_creates_no_account` — call `sso/callback?code=x&state=y` directly, no prior `sso/login` call (no cookie sent); assert 302 to `.../login?error=sso_failed`; assert no new account exists.
  - [x] `SsoCallback_mismatched_state_redirects_to_login_with_error_and_creates_no_account` — call `sso/login`, then `sso/callback` with the real cookie but a different `state` query value.
  - [x] `SsoCallback_missing_code_redirects_to_login_with_error` — valid state, no `code` param.
  - [x] `SsoCallback_when_ssoClient_throws_redirects_to_login_with_error_and_creates_no_account` — set `FakeSsoClient.ThrowOnExchange`, run a valid state+code callback, assert the failure redirect and no account created.
  - [x] `SsoCallback_reuses_ssoState_cookie_only_once` — run one full successful flow, then replay the exact same callback request (same cookie value, if still held by the test's `HttpClient`) a second time; since the cookie was deleted server-side after first use, assert the second attempt fails (missing-state path) rather than succeeding again or double-creating an account.
  - [x] Backend suite must stay green (`dotnet test`) — no regressions to the 272/272 baseline from Story 4.1.

- [x] **Task 6: Check `deferred-work.md` — this story is the first real trigger for two open items** (retro discipline, standing practice since Epic 1)
  - [x] Re-read `deferred-work.md` in full at kickoff (already done for this story's own creation — re-confirm at dev time).
  - [x] The deferred item *"`provider`/`subjectId` aren't trimmed/normalized like `email` is... revisit only if story 4.2's OAuth client shows real whitespace/casing drift"* — this story is that trigger point. Since `FakeSsoClient`'s `SubjectId` is developer-controlled test data (no real drift possible) and the real `ZPaxSsoClient`'s actual response shape is itself an open question (Task 2), there is no real evidence of drift yet — **note as "checked, still no evidence, remains deferred"** rather than fixing speculatively. Confirmed: the real `ZPaxSsoClient.ExchangeCodeForIdentity` maps `SubjectId` from a numeric `id` field stringified via `.ToString()` — no whitespace/casing surface exists at all for this field.
  - [x] The deferred item about re-linking an already-linked account silently rebinding — still not reachable (single provider, and this story's own repeat-login case is a same-identity re-set, not a rebind-to-different-identity case) — **note as "checked, still not applicable."**
  - [x] No other open item touches the Auth domain's OAuth surface.

- [x] **Task 7: Verify CI green and branch/PR**
  - [x] Branch as `story/4.2-zpax-oauth-login-flow` from `main`. (Already checked out at kickoff.)
  - [ ] Push and confirm both CI jobs (Backend .NET, Frontend Vite/React) green before merging (AD-11). This story makes no frontend changes (Story 4.3's job) — frontend job should pass through unaffected. **Left for Jack** — per standing project practice, push/PR/CI verification steps are his to run and approve individually, not performed by the dev agent.

### Review Findings

- [x] [Review][Patch] Account takeover via unverified email SSO linking — `CreateOrLinkSsoAccount` (pre-existing, Story 4.1) links to any existing account purely by email match; this diff is what makes that logic reachable by an unauthenticated external actor for the first time, via `SsoCallback`. z-pax's own `/connect/userinfo` response includes `emailVerified` (per this story's own API-reference note), but `ZPaxUserInfoResponse`/`ExchangeCodeForIdentity` never captures or checks it. **Fixed:** `ZPaxUserInfoResponse` now captures `EmailVerified`; `ExchangeCodeForIdentity` throws when it's `false` (routes to the existing generic failure redirect). New tests: `ExchangeCodeForIdentity_throws_when_email_is_not_verified`. [backend/BarbershopApi/Services/ZPaxSsoClient.cs:60-69, backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] `ssoState` is single-use only via cookie deletion, not tracked server-side — concurrent/replayed requests carrying the same still-valid cookie+state before the deleting `Set-Cookie` reaches the browser could both pass the equality check and both proceed to account creation/linking. Mitigated in practice by z-pax's own authorization-code single-use enforcement, but not guaranteed by our code. **Fixed, then corrected in round 2** (see Round 2 below): added `ISsoStateStore`/`InMemorySsoStateStore`, a one-time nonce store; `SsoCallback` calls `TryConsume(state)` and rejects if already consumed. New test: `SsoCallback_state_already_consumed_by_a_concurrent_request_redirects_to_login_with_error`. [backend/BarbershopApi/Services/ISsoStateStore.cs, backend/BarbershopApi/Controllers/AuthController.cs]
- [x] [Review][Patch] Failure-redirect URL duplicated as two independent literals with no shared source of truth [backend/BarbershopApi/Controllers/AuthController.cs:18, backend/BarbershopApi/Services/ZPaxSsoClient.cs:10] — Story 4.3 depends on this exact value; extract to one shared constant to prevent drift. **Fixed:** added `SsoRedirects.Failure` constant in `ISsoClient.cs`; both `AuthController` and `ZPaxSsoClient` now reference it instead of their own literals.
- [x] [Review][Patch] `SameSite=Lax` (this story's own flagged highest-risk regression point) is never asserted in `SsoLogin_redirects_to_authorize_endpoint_with_state_cookie` [backend/BarbershopApi.Tests/AuthControllerTests.cs] — only `HttpOnly` is checked; add `SameSite`/`Secure` assertions so the exact regression the Dev Notes warn about would actually fail the suite. **Fixed:** test now also asserts `samesite=lax`, `secure`, and `path=/api/auth/sso`.
- [x] [Review][Patch] The real `ZPaxSsoClient` implementation has zero test coverage — every test substitutes `FakeSsoClient` [backend/BarbershopApi/Services/ZPaxSsoClient.cs]; add unit tests using a fake `HttpMessageHandler` to cover URL building and the token/userinfo exchange. **Fixed:** new `ZPaxSsoClientTests.cs` — `BuildAuthorizationUrl` query params, the token→userinfo exchange sequence, `redirect_uri` reuse, missing/empty-field and unverified-email rejection, and a non-2xx token-endpoint response. [backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] `ssoState` cookie has no explicit `Path` [backend/BarbershopApi/Controllers/AuthController.cs:111-117] — works today only via the implicit default-path coincidence of both routes sharing `/api/auth/sso/`; set `Path = "/api/auth/sso"` explicitly. **Fixed:** `Path = "/api/auth/sso"` set on both `Append` and the matching `Delete`.
- [x] [Review][Patch] No timeout/cancellation on outbound z-pax HTTP calls [backend/BarbershopApi/Services/ZPaxSsoClient.cs:42,51, backend/BarbershopApi/Program.cs] — a hung z-pax endpoint stalls the user's request up to the default 100s `HttpClient` timeout; add `ConfigureHttpClient` with an explicit timeout. **Fixed:** `AddHttpClient<ISsoClient, ZPaxSsoClient>` now sets a 10-second `client.Timeout`. [backend/BarbershopApi/Program.cs]
- [x] [Review][Patch] Zero server-side logging anywhere in the SSO flow [backend/BarbershopApi/Controllers/AuthController.cs, backend/BarbershopApi/Services/ZPaxSsoClient.cs] — every failure branch is silently swallowed into the generic redirect; add `ILogger` calls on each failure path. **Fixed, scoped to SSO only** (per Jack's explicit direction — every other Controller/Service in this codebase still has zero logging, an intentionally deferred codebase-wide decision per every prior review since Story 1.4; this introduces `ILogger<AuthController>`/`ILogger<ZPaxSsoClient>` as the first instance, not yet a project-wide pattern). `LogWarning` added on every SSO failure branch in both classes.
- [x] [Review][Patch] `SsoLogin` has no failure guard around `ssoClient.BuildAuthorizationUrl(state)` [backend/BarbershopApi/Controllers/AuthController.cs:119] — wrap in try/catch for parity with every other failure branch in this story. **Fixed:** wrapped in try/catch, redirecting to `SsoRedirects.Failure` on exception; the `ssoState` cookie is now only set after the URL builds successfully.
- [x] [Review][Patch] `ZPaxSsoOptions` is never validated at startup [backend/BarbershopApi/Services/ZPaxSsoOptions.cs, backend/BarbershopApi/Program.cs:44] — add `ValidateDataAnnotations().ValidateOnStart()` so a missing env var fails fast instead of silently sending malformed OAuth requests. **Fixed with an adjustment found during implementation:** `ValidateOnStart()` on `ClientId`/`ClientSecret` would have broken every test and CI run — those two are env-var-only secrets (never in `appsettings.json`, same convention as `AdminSeed`) and are legitimately blank wherever `FakeSsoClient` stands in for the real client. `AuthorizationEndpoint`/`TokenEndpoint`/`UserInfoEndpoint`/`RedirectUri` (always populated in `appsettings.json` in every environment) now use `.Validate(...).ValidateOnStart()`; `ClientId`/`ClientSecret` get a non-fatal `LogWarning` at startup instead, mirroring `AdminBootstrapService`'s existing log-and-skip pattern for the same class of optional secret. [backend/BarbershopApi/Program.cs]
- [x] [Review][Patch] Userinfo null-check only rejects literal `null`, not empty string [backend/BarbershopApi/Services/ZPaxSsoClient.cs:56] — z-pax returning `""` for email/firstName/lastName creates an account with empty identity fields; use `string.IsNullOrEmpty`. **Fixed.** New tests: `ExchangeCodeForIdentity_throws_when_a_required_identity_field_is_missing_or_empty` (theory, covers `null` and `""`). [backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] `userInfo.Id` (non-nullable `int`) silently defaults to `0` if the field is absent/malformed, with no validation that it was actually present [backend/BarbershopApi/Services/ZPaxSsoClient.cs:61,71]. **Fixed:** `ZPaxUserInfoResponse.Id` is now `int?`; missing/absent `id` is rejected by the same required-field check as email/firstName/lastName.
- [x] [Review][Patch] Asymmetric exception handling: the `LoginViaSso` call only catches `AdminAccountProtectedException`/`SsoIdentityConflictException`, unlike the sibling `ExchangeCodeForIdentity` call's `catch (Exception)` — any other exception (e.g. an unexpected DB failure) propagates as an unhandled 500 instead of the documented generic failure redirect [backend/BarbershopApi/Controllers/AuthController.cs:151-163]. **Fixed:** added a trailing `catch (Exception)` after the two typed catches, matching the sibling call's pattern.
- [x] [Review][Patch] AC #5's "no account created" invariant is untested for the missing-`code` failure path [backend/BarbershopApi.Tests/AuthControllerTests.cs] — structurally unreachable today (the code-check runs before any account logic), but add the assertion for parity with sibling tests and regression-proofing. **Fixed:** added the missing `Assert.Null(...)` to `SsoCallback_missing_code_redirects_to_login_with_error`.
- [x] [Review][Defer] `Secure=true` cookies are incompatible with the `http` launchSettings profile [backend/BarbershopApi/Properties/launchSettings.example.json, backend/BarbershopApi/Controllers/AuthController.cs:114] — deferred, pre-existing pattern from the original `Login` endpoint's `refreshToken` cookie (Story 1.5); this diff only extends the same convention to the new `ZPaxSso` env vars.

#### Round 2 (re-review of the round-1 fixes themselves)

- [x] [Review][Patch] `InMemorySsoStateStore.TryConsume`'s `IMemoryCache.TryGetValue`+`Set` pair is not atomic — a genuine check-then-act race reopening the exact concurrent-replay window the store was built to close (two simultaneous callback requests carrying the same `state` could both observe a cache miss before either writes). **Fixed:** rebuilt on `ConcurrentDictionary<string, DateTimeOffset>.TryAdd` (a truly atomic add-if-absent), with opportunistic expired-entry cleanup on each call instead of relying on `IMemoryCache`'s TTL. [backend/BarbershopApi/Services/ISsoStateStore.cs]
- [x] [Review][Patch] Ordering bug introduced by the round-1 fix: `ssoStateStore.TryConsume(state)` ran *before* the missing-`code` check, so a codeless callback (link-prefetch, denied-consent redirect) would burn the one-time nonce and cause the user's real subsequent callback to fail as "already consumed." **Fixed:** reordered so the `code` presence check runs first; the nonce is only consumed once the request is about to proceed to the expensive identity-exchange path. [backend/BarbershopApi/Controllers/AuthController.cs]
- [x] [Review][Patch] `isLocked` — present in z-pax's own documented userinfo response shape (Task 2's AC notes) — was never captured or checked, an analogous account-bypass gap to the `emailVerified` one fixed in round 1. **Fixed:** `ZPaxUserInfoResponse.IsLocked` added; `ExchangeCodeForIdentity` throws when `true`, applying the same treatment already approved for `emailVerified`. New test: `ExchangeCodeForIdentity_throws_when_account_is_locked`. [backend/BarbershopApi/Services/ZPaxSsoClient.cs, backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] Every SSO failure branch logged at `LogWarning`, including genuinely unexpected server-side faults (e.g. an unforeseen exception resolving the local account) — indistinguishable from routine client-input rejections (missing/mismatched state, missing code) to any severity-based alerting. **Fixed:** the two branches that represent unexpected faults rather than routine rejections — `SsoLogin`'s catch around `BuildAuthorizationUrl` and `SsoCallback`'s trailing catch around `LoginViaSso` — now log at `LogError`; routine client-driven rejections remain `LogWarning`. [backend/BarbershopApi/Controllers/AuthController.cs]
- [x] [Review][Patch] Identity-field validation used `string.IsNullOrEmpty`, not `IsNullOrWhiteSpace` — a whitespace-only `firstName`/`lastName`/`email` would pass and get persisted onto the account. **Fixed.** [backend/BarbershopApi/Services/ZPaxSsoClient.cs]
- [x] [Review][Patch] `ZPaxSsoOptions` startup validation (round 1) checked only that the four endpoint/redirect fields were non-blank, not that they were well-formed URLs — a malformed value would still pass `ValidateOnStart()` and only surface later as a generic exception at request time. **Fixed:** each `.Validate(...)` now uses `Uri.TryCreate(o.X, UriKind.Absolute, out _)`. [backend/BarbershopApi/Program.cs]
- [x] [Review][Patch] Missing test: the userinfo endpoint's non-2xx response path had no test (only the token endpoint's did), despite an identical code shape. **Fixed:** `ExchangeCodeForIdentity_throws_when_userinfo_endpoint_returns_an_error_status`. [backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] Missing test: no test asserted the `scope=api` field on the token-exchange POST body, despite it being called out in the Completion Notes as a real-provider-driven fix. **Fixed:** added to the redirect_uri-parity test. [backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] Missing test: no test covered `id` being entirely omitted from the userinfo JSON — the exact shape of the bug the nullable-`Id` fix targeted (only `null`/absent-via-other-fields was covered indirectly). **Fixed:** `ExchangeCodeForIdentity_throws_when_id_field_is_omitted_from_userinfo_response`, using a raw JSON body with no `id` key. [backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] `ExchangeCodeForIdentity_resends_the_same_redirect_uri_used_on_the_authorize_request` was vacuous — it never called `BuildAuthorizationUrl`, so it compared the token-exchange body only against the same `ZPaxSsoOptions` instance both it and the client already shared, and couldn't actually catch the two call sites diverging. **Fixed:** the test now calls `BuildAuthorizationUrl` first and derives the expected value from its output. [backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs]
- [x] [Review][Patch] `Path = "/api/auth/sso"` was duplicated as an independent literal in two places (the cookie `Append` and the separate `Delete` options) — the same anti-pattern round 1 explicitly fixed for the failure-redirect constant. **Fixed:** extracted to a single `SsoStateCookiePath` const, referenced by both. [backend/BarbershopApi/Controllers/AuthController.cs]
- [x] [Review][Dismiss] In-memory-only nonce store has no resilience to cache eviction or app restart — inherent, accepted tradeoff of a lightweight in-memory store at this app's single-instance/demo scale; a durable store would be over-engineering here.
- [x] [Review][Dismiss] `EmailVerified`/`IsLocked` field-name-or-type mismatch risk against z-pax's still-unverified real wire format — same bucket as the already-accepted "pending live verification" risk from round 1 (`scope`/`fallback_uri`); can't be resolved without live z-pax access.
- [x] [Review][Dismiss] `token.AccessToken` unvalidated before use in the userinfo call — pre-existing, already triaged and dismissed in round 1 (self-heals via the downstream `EnsureSuccessStatusCode()` call).
- [x] [Review][Dismiss] SSO access token computed then discarded in `LoginViaSso` — already addressed/accepted in round 1 (intentional AD-3 design; the token reaches the SPA via the existing refresh-bootstrap instead).
- [x] [Review][Dismiss] Cookie deletion doesn't mirror `Secure`/`SameSite` from the original cookie — self-acknowledged functionally inert by the reporting agent (RFC 6265 cookie identity is name+domain+path only).
- [x] [Review][Dismiss] Asymmetric null/empty permutations across the identity-field test theory — diminishing-returns nitpick; existing coverage already proves the general behavior.

## Dev Notes

### Architecture Compliance (must-follow, not optional)

- **AD-19 (z-pax SSO)** — this story implements the OAuth mechanics: `ISsoClient`, the `state`/CSRF cookie flow, and the two new `AuthController` endpoints. **Already done (Story 4.1, out of scope here):** the schema (`SsoProvider`/`SsoSubjectId`/nullable `PasswordHash`) and `AccountRepository.CreateOrLinkSsoAccount`/`FindBySsoIdentity`. **Out of scope for this story:** any frontend UI — the "Sign in with z-pax" button and Login-page error rendering are Story 4.3's job. This story's only frontend-visible surface is the two redirect target URLs (Task 4's `.../login?error=sso_failed` and `.../schedule-appointment`/`.../my-schedule`), which Story 4.3 and the existing route guards respectively already exist to consume.
- **AD-1 (layering)** — one Auth trio, extended: `AuthController` gets two new actions, `AuthService` gets one new method, both reusing existing private helpers. New `ISsoClient`/`ZPaxSsoClient`/`ZPaxSsoOptions` are Auth-domain Service-layer additions, not a new domain concept.
- **AD-2 (Role enum / no ad-hoc literals)** — `SsoProviders.ZPax` constant, not a repeated `"zpax"` string literal.
- **AD-3 (token transport)** — the access token is never put in the URL or a JS-readable cookie during the redirect. It reaches the SPA the same way it does on every fresh page load: the `refreshToken` cookie gets set by the callback response, the browser lands on a normal app route, and `AuthContext.jsx`'s existing `bootstrap()` effect (already built, runs on every mount) calls `POST /api/auth/refresh` → `GET /api/auth/me` to hydrate the in-memory access token and identity — exactly the same mechanism a plain page-refresh already uses. **This story adds no new frontend code and needs none** — the existing bootstrap effect is a passive beneficiary of the AC #3 "mints tokens exactly as Login does" requirement, not something this story needs to touch.
- **AD-4/NFR4 (testing)** — real temp SQLite via `WebApplicationFactory`/`SqliteApiFactory`; the *only* thing faked is the external z-pax boundary (`ISsoClient`), never the database.
- **AD-5 (login rate limiting)** — scoped explicitly to `/api/auth/login` only; the new `sso/login`/`sso/callback` GET endpoints are not in scope for rate limiting per any AD, and this story does not add any.
- **AD-13 (CORS)** — unaffected. The browser's redirects to/from z-pax and to the frontend are top-level navigations, not `fetch`/XHR calls, so CORS doesn't apply to them at all (CORS only governs cross-origin script-initiated requests).
- **AD-18 (client-side routing)** — unaffected; existing `RequireRole` guards on `/schedule-appointment` and `/my-schedule` apply exactly as they do for a normal Login-then-redirect user, once `AuthContext` has bootstrapped.

### Design Decisions This Story Must Make (epics/architecture leave these open)

- **`ssoState` cookie must be `SameSite=Lax`, never `Strict`.** See Task 4 — this is the single highest-risk, easiest-to-get-silently-wrong detail in this story, because `refreshToken`'s existing `SameSite=Strict` is the pattern immediately at hand to copy, and copying it here would make `state` validation fail on every real browser flow while every automated test (which never performs a true cross-site top-level navigation) would still pass. Structural cause: `Strict` cookies are withheld by the browser on cross-site top-level navigations (z-pax → our callback); `Lax` cookies are still sent on that specific navigation type (top-level GET).
- **The frontend redirect target is hardcoded, not newly configurable.** `https://localhost:5173` is already hardcoded in `Program.cs`'s CORS policy and mirrored in the fixed `redirect_uri` AD-19 itself specifies — adding a third, independent config surface for the same fixed local value would be inconsistent, not more flexible (NFR7 rules out ever needing a second value).
- **`CreateOrLinkSsoAccount` alone is sufficient per callback — do not also call `FindBySsoIdentity`.** See Task 3's explicit note; verified by reading the current `AccountRepository.cs`, this overrides a speculative note left in Story 4.1's own Dev Notes.
- **The real z-pax identity-fetch mechanism is confirmed, not guessed:** a separate `GET /connect/userinfo` call with the token-endpoint's `access_token` as a Bearer credential (`scope=profile` required on the authorize request for this reason) — not fields embedded in the token response. See Task 2. This overrides the credentials-not-yet-available placeholder assumption an earlier draft of this story carried.
- **Token exchange must resend the same `redirect_uri`** used on the authorize request — a standard OAuth2 provider check; the architecture doesn't call this out explicitly, but omitting it is a common source of a real (not simulated) integration failure that no test using `FakeSsoClient` would ever catch.

### Testing Requirements

- xUnit.v3 + `WebApplicationFactory` (`SqliteApiFactory`), no mocked `DbContext` — same as every prior story.
- `AllowAutoRedirect = false` **and** `BaseAddress = https://localhost` are both required on the test `HttpClient` for every SSO test (see Task 5) — the default `HttpClient` behavior of transparently following 302s would make it impossible to assert on `Location`/`Set-Cookie` from the redirect responses themselves; it would also break on the `sso/login`→`sso/callback`→frontend three-hop chain (the last hop points at a non-existent `https://localhost:5173` origin from the test process's point of view); and without the `https://localhost` base, `Program.cs`'s `UseHttpsRedirection()` would intercept every request with its own redirect before any SSO logic runs at all.
- Cross-request cookie handling in tests: `HttpClient`'s default `CookieContainer` will carry the `ssoState` cookie from the `sso/login` response to a subsequent `sso/callback` call on the *same* `HttpClient` instance automatically — reuse one client across both calls within a test rather than creating two.

### Project Structure Notes

- `Controllers/AuthController.cs`, `Services/AuthService.cs`, `Services/IAuthService.cs`, `Program.cs`, `appsettings.json`, `Properties/launchSettings.json`, `Properties/launchSettings.example.json` are all **modified**, not new.
- New: `Services/ISsoClient.cs` (interface + `SsoIdentity` record), `Services/ZPaxSsoClient.cs`, `Services/ZPaxSsoOptions.cs`, `Services/SsoProviders.cs`, `BarbershopApi.Tests/TestOnly/FakeSsoClient.cs`.
- No `Entities/`, `Migrations/`, `Repositories/`, `Dtos/`, or frontend changes at all in this story — Story 4.1 already built the data/repository layer this story calls into; Story 4.3 owns the frontend UI. This story is purely the Auth Service/Controller OAuth-mechanics slice, matching AD-19's own scoping.

### Previous Story Intelligence (Story 4.1)

- `IAccountRepository.CreateOrLinkSsoAccount(email, firstName, lastName, provider, subjectId)` and `FindBySsoIdentity(provider, subjectId)` already exist and are fully tested (7 repository tests, Story 4.1). `CreateOrLinkSsoAccount` throws `AdminAccountProtectedException` if the matched-by-email account is the admin, and `SsoIdentityConflictException` on a unique-index race — both already exist as exception types in `Services/`.
- `Account.PasswordHash` is already nullable; `AuthService.Login` already null-guards it (Story 4.1, Task 3) — this story's new `LoginViaSso` path never touches `PasswordHash` at all, so no further nullability work is needed here.
- Story 4.1's Dev Agent Record flagged the SQLite-trigger migration risk as the highest-risk part of *that* story — not relevant here, no migrations in this story.
- Established test-fixture convention across every Auth/Account test file: `IDisposable` class holding `private readonly SqliteApiFactory _factory = new();`, `MethodName_condition_expectedOutcome` naming, local `New...(...)` factory helpers with named optional parameters, test fixture names use "John Smith"-style placeholders, never a real person's name.

### Git Intelligence Summary

Recent commits: `b097454` (Story 4.1 merged via PR #19) → `2beccbd` (Story 4.1 created) → `f53dd8e`/`5204ab6` (Epic 4 added via course-correct). `b097454` is `main`'s current tip and this story's baseline. Established rhythm across every prior story: create the story on `main`, implement on `story/{epic}.{story}-{slug}`, PR with a summary, merge once both CI jobs are green, delete the branch — Task 7 follows this unchanged.

### References

- [Source: _bmad-output/planning-artifacts/epics.md §Epic 4, §Story 4.2] — story statement, five acceptance criteria
- [Source: _bmad-output/planning-artifacts/architecture/architecture-bmad-learning-project-2026-07-23/ARCHITECTURE-SPINE.md#AD-19] — full OAuth flow rule this story implements the mechanics of (state/CSRF, token exchange, env-var secrets, non-secret endpoint config)
- [Source: _bmad-output/planning-artifacts/sprint-change-proposal-2026-08-19.md §Decision Log, §5.2] — z-pax endpoint URLs, redirect URI, decision to add `state` post-approval, session-mechanism decision (one-time handshake, mint own tokens)
- [Source: _bmad-output/implementation-artifacts/4-1-account-schema-and-sso-aware-repository.md] — `CreateOrLinkSsoAccount`/`FindBySsoIdentity` contracts and behavior this story calls into; its own Dev Notes' speculative (and here overridden) guidance about calling `FindBySsoIdentity` first
- [Source: backend/BarbershopApi/Controllers/AuthController.cs, Services/AuthService.cs, Services/IAuthService.cs, Program.cs, appsettings.json, Properties/launchSettings.json] — exact existing Login/Refresh implementation and config conventions this story extends (cookie options, `JwtOptions`/`AdminSeed` env-var pattern, rate-limiter policy registration style)
- [Source: backend/BarbershopApi/Repositories/AccountRepository.cs:105-141] — read directly to confirm `CreateOrLinkSsoAccount`'s exact idempotent-repeat-login behavior, overriding Story 4.1's speculative note
- [Source: frontend/src/context/AuthContext.jsx, frontend/src/App.jsx, frontend/src/landingRoutes.js] — confirms the existing refresh-on-mount bootstrap this story's redirect-only backend flow relies on, and the exact role→route mapping the callback's success redirect must mirror
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — two open items this story is the first real trigger for (provider/subjectId normalization, re-link rebinding) — checked, both remain correctly deferred (Task 6)
- [Source: project-context.md §Technology Stack & Versions; §Framework-Specific Rules; §Testing Rules; §Critical Don't-Miss Rules]
- [Source: z-pax `GET /connect/userinfo` API reference, provided directly by Jack, 2026-08-24] — confirms the real identity-fetch contract (`scope=profile`, Bearer-authenticated GET, response shape `{id, email, firstName, lastName, phoneNo, emailVerified, hasMetadata, isLocked, avatar, createdAt}`, all name/email fields nullable) — resolves Task 2's previously-open question

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via the bmad-dev-story workflow.

### Debug Log References

- `dotnet build` (backend): 0 warnings, 0 errors.
- `dotnet test` (backend): 280/280 passed (272 baseline from Story 4.1 + 8 new SSO tests), no regressions.

### Completion Notes List

- Implemented the full z-pax OAuth login flow: `ZPaxSsoOptions` config (Task 1), `ISsoClient`/`ZPaxSsoClient`/`SsoProviders`/`FakeSsoClient` (Task 2), `AuthService.LoginViaSso` reusing existing private token-minting helpers (Task 3), and `AuthController`'s `GET /api/auth/sso/login` + `GET /api/auth/sso/callback` endpoints (Task 4).
- `ssoState` cookie uses `SameSite=Lax` (not `Strict`) per the story's explicit design decision — verified this is required for the cross-site top-level navigation z-pax's redirect performs.
- z-pax's `/connect/userinfo` endpoint is hardcoded in `ZPaxSsoClient` (not added to `ZPaxSsoOptions`) since the story's Task 1 config surface only specifies `AuthorizationEndpoint`/`TokenEndpoint`/`RedirectUri`.
- Task 5: added `FakeSsoClient` test double, wired into `SqliteApiFactory` via `RemoveAll<ISsoClient>()` + singleton registration, and 8 new tests in `AuthControllerTests.cs` covering the happy path (new customer, existing barber link-by-email), every AC #5 failure branch (missing/mismatched state, missing code, `ISsoClient` throwing), and single-use `ssoState` cookie replay protection.
- Task 6: re-checked both open `deferred-work.md` items this story was flagged as the trigger for — `SubjectId` normalization (still no evidence: the real client stringifies a numeric `id`, no whitespace/casing surface) and SSO re-link rebinding (still unreachable — single provider, same-identity re-set only). Both remain correctly deferred; no new entry needed.
- Task 7: branch `story/4.2-zpax-oauth-login-flow` was already checked out at kickoff. Push/CI verification/PR/merge intentionally left for Jack per standing project practice (and the standing instruction to pause before commit/push/PR).
- No frontend changes in this story, per its explicit scope (Story 4.3 owns the UI) — frontend CI job is an unaffected pass-through.
- **Post-implementation fix (2026-08-24):** Jack shared z-pax's actual `/connect/token` API reference, which surfaced two real-provider mismatches invisible to `FakeSsoClient`-backed tests: (1) the token response's access-token field is camelCase `accessToken`, not the OAuth2-standard snake_case `access_token` my initial implementation assumed — the explicit `[JsonPropertyName("access_token")]` attribute would have silently left `AccessToken` empty against the real endpoint, since case-insensitive matching doesn't bridge an underscore; removed the attribute so `PropertyNameCaseInsensitive` matches `AccessToken`↔`accessToken` the same way it already does for the userinfo endpoint's `FirstName`/`LastName`. (2) added the `scope` form field to the token-exchange POST body per z-pax's documented requirement for the `authorization_code` grant type, which the story's own Task 2 notes hadn't called out. Both fixed in `ZPaxSsoClient.cs`; full suite re-verified green (280/280).
- **Post-implementation fix #2 (2026-08-24):** Jack shared z-pax's actual `/connect/authorize` API reference, surfacing two more corrections: (1) `scope=offline_access profile` (this story's original AC #1 assumption) is replaced with `scope=api` — z-pax's doc lists `scope`'s value the same single-word way it lists `response_type`'s literal `code`, indicating `api` is a required literal value rather than free-form OAuth scopes; (2) `fallback_uri` is a required param this story's implementation was missing entirely, which would have made every real authorize call fail with 400. Per Jack's direction, `fallback_uri` is set to this story's own `error=sso_failed` login-page redirect (its purpose is undocumented in z-pax's reference beyond "used when the user isn't authenticated/hasn't consented," so this is a reasonable default, not a confirmed-correct value) and `scope=api` is applied to both the authorize URL and the token-exchange POST for consistency. **Both changes are explicitly flagged pending live verification** once real z-pax credentials are available — no way to confirm `scope=api` alone still grants `/connect/userinfo` access, or observe z-pax's actual `fallback_uri` behavior, without a live trial. Full suite re-verified green (280/280).
- **Post-implementation cleanup (2026-08-24):** Jack asked why `UserInfoEndpoint` was hardcoded in `ZPaxSsoClient.cs` while `AuthorizationEndpoint`/`TokenEndpoint` were configurable via `ZPaxSsoOptions` — there was no principled reason, just an asymmetry inherited from Task 1's original config-field list. Moved `UserInfoEndpoint` into `ZPaxSsoOptions`/`appsettings.json` alongside the other two endpoint URLs for consistency; no behavior change (same literal URL value). Suite re-verified green (280/280).

### File List

**Backend — new:**
- `backend/BarbershopApi/Services/ZPaxSsoOptions.cs`
- `backend/BarbershopApi/Services/ISsoClient.cs`
- `backend/BarbershopApi/Services/ISsoStateStore.cs`
- `backend/BarbershopApi/Services/ZPaxSsoClient.cs`
- `backend/BarbershopApi/Services/SsoProviders.cs`
- `backend/BarbershopApi.Tests/TestOnly/FakeSsoClient.cs`
- `backend/BarbershopApi.Tests/ZPaxSsoClientTests.cs`

**Backend — modified:**
- `backend/BarbershopApi/Program.cs`
- `backend/BarbershopApi/appsettings.json`
- `backend/BarbershopApi/Properties/launchSettings.json`
- `backend/BarbershopApi/Properties/launchSettings.example.json`
- `backend/BarbershopApi/Services/IAuthService.cs`
- `backend/BarbershopApi/Services/AuthService.cs`
- `backend/BarbershopApi/Controllers/AuthController.cs`
- `backend/BarbershopApi.Tests/SqliteApiFactory.cs`
- `backend/BarbershopApi.Tests/AuthControllerTests.cs`

## Change Log

- 2026-08-24: Implemented Story 4.2 (Tasks 1-7) — full z-pax OAuth login mechanics: `ISsoClient` abstraction with a real `ZPaxSsoClient` (authorize-URL builder + two-call token exchange/userinfo fetch) and a `FakeSsoClient` test double, new `AuthController` `sso/login`/`sso/callback` endpoints (CSRF `state` cookie with the critical `SameSite=Lax` decision, single-use cookie deletion, generic `error=sso_failed` failure redirect), and `AuthService.LoginViaSso` reusing existing token-minting helpers. Backend suite green (280/280, 8 new tests, no regressions to the 272/272 baseline). No frontend changes — purely the Auth Service/Controller OAuth-mechanics slice, per this story's explicit scope (Story 4.3 owns the UI). Task 7 (push/PR/CI verification) intentionally left for Jack per standing project practice.
- 2026-08-24: Fixed two real-provider mismatches in `ZPaxSsoClient.cs` after Jack shared z-pax's actual `/connect/token` API reference: token response field is camelCase `accessToken` (not snake_case `access_token`, which the initial implementation had hardcoded and which would have silently broken every real login while all `FakeSsoClient`-backed tests kept passing); and the token-exchange POST now includes the required `scope` field. Suite re-verified green (280/280).
- 2026-08-24: Fixed two more real-provider mismatches after Jack shared z-pax's actual `/connect/authorize` API reference: AC #1's `scope=offline_access profile` assumption replaced with `scope=api` (z-pax's doc format indicates this is a literal required value), and the previously-missing required `fallback_uri` param added, defaulted to this story's own `error=sso_failed` login redirect pending live verification against real z-pax credentials. Suite re-verified green (280/280).
- 2026-08-24: Moved `UserInfoEndpoint` from a hardcoded `ZPaxSsoClient.cs` constant into `ZPaxSsoOptions`/`appsettings.json`, for consistency with `AuthorizationEndpoint`/`TokenEndpoint` (no principled reason for the original asymmetry). No behavior change. Suite re-verified green (280/280).
- 2026-08-24: Code review round — 3 adversarial/edge-case/acceptance-audit layers found 2 decision-needed and 12 patch findings (plus 1 deferred, 7 dismissed as pre-documented or already-handled). Jack resolved both decisions to "patch now" and elected to apply all 14 fixes: `EmailVerified` capture/enforcement closing an unverified-email account-takeover path opened by this story (the underlying `CreateOrLinkSsoAccount` email-match logic is pre-existing from Story 4.1, but this story is what first makes it reachable by an unauthenticated external actor); a new `ISsoStateStore` in-memory nonce store closing a concurrent-replay race on the CSRF `state` value; a shared `SsoRedirects.Failure` constant (was duplicated in two files); `SameSite`/`Secure`/`Path` cookie-attribute test assertions; a new `ZPaxSsoClientTests.cs` giving the real HTTP-calling client its first test coverage (previously 100% substituted by `FakeSsoClient`); an explicit `ssoState` cookie `Path`; a 10s `HttpClient` timeout; `ILogger<AuthController>`/`ILogger<ZPaxSsoClient>` logging scoped to the SSO endpoints only (an explicit, narrower choice than the codebase-wide logging decision every prior review has deferred since Story 1.4); a pre-URL-build failure guard on `sso/login`; `ZPaxSsoOptions` startup validation split between fail-fast (`ValidateOnStart` on the four always-configured endpoint/redirect URLs) and a non-fatal warning (`ClientId`/`ClientSecret`, env-var-only secrets legitimately blank in test/CI, mirroring `AdminBootstrapService`'s existing log-and-skip pattern — a `[Required]`/`ValidateOnStart` on those two would have broken every test); empty-string and missing-`id` validation on the userinfo response; a widened `catch (Exception)` on `LoginViaSso` matching its sibling call's pattern; and a missing test assertion on the already-unreachable no-account-created path. Suite green: 289/289 (280 baseline + 9 new tests). Sprint status corrected: this story was already `Status: review` in its own frontmatter but `sprint-status.yaml` still showed `ready-for-dev` — synced to `done` alongside this review.
- 2026-08-24: Round 2 code review — re-ran the same 3-layer review against the round-1 fixes themselves (not just the original implementation), on the explicit premise of not trusting the "Fixed:" claims without re-verifying against the diff. Found a real regression the round-1 fix introduced (the state nonce was consumed before the missing-`code` check, able to burn a legitimate state token) and a real gap in the round-1 fix's own correctness (`InMemorySsoStateStore.TryConsume`'s `IMemoryCache` `TryGetValue`+`Set` pair was a non-atomic check-then-act, not actually closing the concurrent-replay race it was built for) — rebuilt on `ConcurrentDictionary.TryAdd` instead. Also applied: an `isLocked` check mirroring the already-approved `emailVerified` treatment (same gap class, same field family, missed in round 1); `LogError` (not `LogWarning`) on the two branches representing genuinely unexpected faults; `IsNullOrWhiteSpace` instead of `IsNullOrEmpty` on identity fields; URL-format validation (not just non-blank) on `ZPaxSsoOptions`'s startup check; a fix to a vacuous test that never called the method it claimed to test; three new tests (userinfo non-2xx, `id` omitted, account locked); and a shared `SsoStateCookiePath` constant replacing a duplicated literal. 6 findings dismissed as accepted tradeoffs or already-addressed in round 1. Suite green: 292/292 (289 + 3 new tests).
